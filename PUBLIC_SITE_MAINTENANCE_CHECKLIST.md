# Public Site Maintenance Verification Checklist

## 코드 반영 확인

- [x] `public_site_controls` 테이블 생성 로직 추가
- [x] `cgma.or.kr` 기본 임시페이지 모드 등록
- [x] 기본 안내 화면 생성 로직 추가
- [x] 지정 주소 연결 방식 추가
- [x] 버튼 이동과 자동 이동 분기 추가
- [x] 관리자 API 추가
- [x] 관리자 UI 모듈 추가
- [x] 관리자 인증 후 UI 로드 목록에 모듈 추가

## 운영 확인 항목

배포 후 아래를 확인한다.

1. `https://cgma.or.kr/` 접속 시 `현재 사이트 개발중입니다`가 표시되는지 확인
2. `https://admin.ekodi.kr/#public-site-controls` 접속 후 로그인
3. 좌측 메뉴에 `공개상태` 항목 표시 확인
4. `CGMA / cgma.or.kr` 설정 카드 표시 확인
5. 공개 상태를 `임시페이지`로 저장 후 사이트 확인
6. 임시페이지 방식을 `지정 주소 연결`로 변경
7. 지정 주소 입력 후 버튼 이동 확인
8. 연결 방식을 `자동 이동`으로 변경 후 리디렉션 확인
9. 다시 `기본 안내 화면`으로 되돌리기
10. 필요 시 `정상 공개` 전환 확인

## 주의

현재 기능은 코드 반영 상태이며, 실제 외부 도메인 적용은 `cgma.or.kr`이 이 Worker 라우트로 연결되어 있어야 즉시 보인다.

Cloudflare route 또는 DNS가 다른 Worker/호스팅으로 연결되어 있다면, 해당 라우팅 연결을 별도로 맞춰야 한다.
