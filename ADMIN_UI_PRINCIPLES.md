# EKODI 관리자 UI 원칙 v3 · 8세대

관리자 화면은 ‘관제센터처럼 보이는 것’보다 ‘빠르게 읽고 판단하고 실행하는 것’을 우선한다. 8세대 관리자 UI는 페이지들의 모음이 아니라 하나의 운영 작업대다. 모든 관리자 하위서비스는 같은 Shell, Navigation Registry, Design Engine, Guardrail을 상속하고 서비스는 작업 내용만 소유한다.

## 0. 8세대 공통 쉘은 네 영역과 하나의 스크롤 주체로 구성한다
1. **좌측 1차 내비게이션** — `홈 · 운영 · 공간 · 서비스 · 시스템` 다섯 축만 표시한다. 데스크톱에서는 뷰포트에 고정하고 스크롤시키지 않는다.
2. **우측 상단 맥락 내비게이션** — 선택한 1차 축에 속한 세부메뉴만 가로 탭으로 표시한다. 다른 축의 메뉴는 숨긴다.
3. **작업공간 헤더** — 현재 작업의 제목, 목적, 상태, 핵심 조작을 한 줄의 문맥으로 묶는다. 서비스가 전역 헤더를 새로 만들지 않는다.
4. **중앙 작업공간** — 표, 폼, 카드, 편집기, 지도, 로그 등 실제 일을 수행하는 영역이다.

스크롤은 중앙 작업공간 하나만 소유한다. `body`, 앱 프레임, 좌측 1차 메뉴, 맥락 탭은 독립 세로 스크롤의 주체가 아니다. 표나 대용량 데이터 뷰처럼 명시적으로 필요한 곳만 제한적 내부 스크롤을 허용한다.

> 구조 요약: **왼쪽은 길, 위는 문맥, 헤더는 현재 일, 가운데는 실행**.

## 1. 전역 내비게이션은 다섯 축으로 고정한다
관리자 전역의 1차 메뉴는 다음 다섯 개만 사용한다.

1. `홈` — 관리자 진입점과 현재 핵심 상태
2. `운영` — 지금 진행 중인 일, 업무, 결제·회계, 세금·증빙, 소통
3. `공간` — 조직, 고객, 클라우드 자료와 운영공간
4. `서비스` — 마케팅·AI·출판·커뮤니티·소셜 등 개별 서비스
5. `시스템` — 건강, 보안, 기기, 구조, 관리자 권한, 비용, 저장소

새 기능을 만들 때 여섯 번째 전역 메뉴를 추가하지 않는다. 먼저 다섯 축 가운데 의미가 가장 가까운 곳에 배치한다.

## 2. Context를 먼저 보여준다
- 사용자가 다섯 축 중 하나를 선택하면 그 축에 필요한 하위 도구만 보여준다.
- 모든 하위서비스를 한 번에 나열하지 않는다.
- 현재 위치와 관련 없는 메뉴는 접어 시각적·인지적 부하를 줄인다.
- 최근 사용 기능과 즐겨찾기는 빠른 재진입을 돕는 보조 경로로만 제공한다.

## 3. 서비스의 세부 기능은 서비스 내부에서 해결한다
- 전역 사이드바는 서비스 이름까지만 책임진다.
- 캠페인, 승인, CRM, 정산, 설정처럼 서비스 고유 기능은 해당 서비스의 탭·도구막대·점진적 공개 영역에 둔다.
- 한 서비스의 세부 기능 때문에 전역 메뉴가 늘어나지 않게 한다.

## 4. 권한 없는 기능은 숨기고 경로는 결정적으로 유지한다
- 역할·조직·공간 권한이 없는 메뉴와 조작은 노출하지 않는다.
- AI 명령창은 빠른 지름길이지만 유일한 접근수단이 아니다.
- 동일 작업에는 언제나 사람이 따라갈 수 있는 결정적인 메뉴 경로가 존재해야 한다.
- 삭제·결제·권한·핵심 설정 등 위험 작업은 기존 승인·감사 경계를 유지한다.

## 5. 하나의 Navigation Registry만 사용한다
- 관리자 메인과 모든 하위서비스는 `admin-menu-registry.js`와 공통 Sidebar를 사용한다.
- 개별 서비스가 메뉴 이름, 순서, 그룹, 번역을 독자적으로 복제하지 않는다.
- 새로운 관리자 기능은 Registry에 등록해야 하며 다섯 축 중 하나에 반드시 소속되어야 한다.
- 내부 운영 기능은 필요하면 AI·진단 경로로 연결하되 별도 전역 축으로 승격하지 않는다.

## 6. 정보가 장식보다 먼저다
- 관리자 메인 배경은 밝은 중립색, 데이터·작업 표면은 흰색을 기본으로 한다.
- 큰 면적의 짙은 배경, 그라데이션, 글로우, 과도한 그림자는 사용하지 않는다.
- 짙은 배경은 코드·보안 경고·특수 진단처럼 의미가 분명한 좁은 영역에만 제한한다.

## 7. 한눈에 읽히는 문자 체계
- 본문·표·버튼은 데스크톱에서 13~15px 이상을 기본으로 하고, 핵심 본문은 14~16px를 권장한다.
- KPI 값은 20~28px, 섹션 제목은 17~24px 범위에서 계층을 명확히 한다.
- 보조문구도 11px 미만으로 축소하지 않는다. 운영정보를 ‘작게 숨기는’ 방식은 금지한다.
- 본문 대비는 WCAG AA 수준을 목표로 한다.

## 8. 8px 계열 간격과 낮은 시각 밀도
- 기본 간격은 8px 계열로 구성하고 카드 사이 12~16px, 섹션 안쪽 14~24px를 기준으로 한다.
- 같은 의미의 숫자·상태·조작은 동일한 위치와 리듬으로 반복한다.
- 비어 있는 공간은 정보 구획을 위해 사용하되, 빈 화면을 만들기 위한 큰 최소높이는 두지 않는다.

## 9. 상태는 색이 아니라 문장으로도 이해되어야 한다
- 정상·주의·오류 색은 의미가 있는 상태에만 사용한다.
- 색상만으로 상태를 구별하지 않고 텍스트 라벨을 함께 표시한다.
- 원시 오류코드만 노출하지 않는다. 사용자 문장을 먼저 보여주고 필요하면 기술코드를 보조정보로 둔다.

## 10. 로딩·빈 상태·인증·오류를 화면의 일부로 설계한다
- 빈 데이터 영역을 거대한 공백으로 남기지 않는다.
- 로딩, 데이터 없음, 인증 필요, 연결 대기, 오류를 각각 명시적인 상태 카드로 보여준다.
- 실패 상태에서도 다음 행동이 무엇인지 알 수 있어야 한다.

## 11. 관리자 쉘은 뷰포트에 붙이고, 반응형은 ‘축소’가 아니라 ‘재배치’다
- 데스크톱 관리자 화면은 브라우저 뷰포트 상단에서 바로 시작한다. 공개 사이트의 고정 헤더용 body 상단 여백·오프셋을 관리자 쉘이 상속하지 않는다.
- 좌측 전역 사이드바는 데스크톱에서 뷰포트에 고정되어 본문 스크롤과 함께 위아래로 이동하지 않는다. 기본 스크롤 책임은 본문에 둔다.
- 좌측 사이드바 자체에는 기본적으로 독립 세로 스크롤을 만들지 않는다. 높이가 부족하면 메뉴 밀도 조정, 맥락별 접기, 그룹 전환으로 해결하며 필수 메뉴를 잘라내지 않는다.
- 900px 이하에서는 툴바와 2열 콘텐츠를 1열로 전환한다.
- 620px 이하에서는 KPI를 2열 중심으로 배치하고 조작 버튼은 충분한 터치 영역을 확보한다.
- 표와 탭을 제외하고 중첩 스크롤 영역을 만들지 않는다.
- 다섯 전역 축은 모바일에서도 의미와 순서를 유지한다.

## 12. 경량성이 기능이다
- 첫 관리자 진입에는 공통 쉘과 현재 화면에 필요한 최소 자산만 로드한다.
- 하위서비스 CSS·JS는 메뉴 진입 시 지연 로딩한다.
- UI 개선을 위해 무거운 프레임워크, 아이콘 패키지, 애니메이션 라이브러리를 추가하지 않는다.
- CSS 그림자·블러·배경효과는 최소화하고 브라우저 기본 레이아웃 기능을 우선한다.

## 13. 공통 토큰을 깨지 않는다
- 페이지 배경, 표면, 본문색, 보조색, 테두리, 포인트색, 성공·주의·오류색, 반경, 간격, 컨트롤 높이는 공통 관리자 토큰을 따른다.
- 하위서비스가 고유 브랜드색을 쓸 수는 있지만 공통 가독성·간격·상태 규칙을 덮어쓰지 않는다.

## 14. 배포 전 검증 기준
- 전역 1차 메뉴가 `홈 · 운영 · 공간 · 서비스 · 시스템` 다섯 축인지 자동 검증한다.
- 모든 공개 관리자 메뉴가 다섯 축 중 하나에 소속되는지 검증한다.
- 데스크톱 1366×768, 낮은 높이 1366×600 및 모바일 390×844에서 핵심 정보가 잘리지 않아야 한다.
- 데스크톱에서는 관리자 쉘 상단 공백이 0이고 좌측 전역 사이드바가 본문 스크롤과 독립적으로 뷰포트에 고정되는지 검증한다.
- 본문·KPI·탭·상태 라벨이 육안으로 구별되어야 한다.
- 인증·빈 상태·오류에서 거대한 빈 패널이 생기지 않아야 한다.
- 첫 관리자 경로의 기존 JS/CSS 성능 예산과 true-lazy 계약을 유지해야 한다.
- production 배포 후 `admin.ekodi.kr` 응답, fingerprint, immutable lazy assets와 다섯 축 내비게이션 자산을 확인한다.

## 15. EKODI Design Engine 계층
관리자 UI는 페이지별 커스텀 디자인이 아니라 공통 디자인 엔진을 통해 생성·검증한다.

1. `Research Evidence` — 검증된 관리자 UX와 실제 운영 데이터를 근거로 패턴을 채택한다.
2. `Design Core` — 색, 간격, 타이포그래피, 상태, 표면, 컨트롤의 공통 토큰과 컴포넌트를 제공한다.
3. `Admin Shell` — 고정 좌측 1차 메뉴 → 우측 상단 맥락 세부메뉴 → 중앙 작업공간의 기본 골격을 제공한다.
4. `EKODI Identity Layer` — 에코디 브랜드 표현은 Core를 파괴하지 않는 별도 레이어로 적용한다.
5. `EKODIAN Character Layer` — 에코디언은 안내·빈 상태·오류·온보딩·완료 피드백에 제한적으로 등장하며 핵심 작업 콘텐츠를 가리지 않는다.
6. `Service Theme` — 서비스별 차이는 복제 CSS가 아니라 token, theme, slot, variant로 표현한다.
7. `Guardrails` — 반응형·접근성·성능·내비게이션·레이아웃 계약을 CI와 운영 점검으로 자동 검증한다.

## 16. 에코디 맞춤 레이어는 공통 UX와 분리한다
- 외부 우수 사례에서 가져오는 것은 정보 구조, 사용성 패턴, 반응형 원칙, 접근성 같은 검증 가능한 UX 원칙이다.
- 로고, 시각적 골격의 세부 리듬, 캐릭터, 서비스별 분위기 등은 `EKODI Identity Layer`에서 관리한다.
- 벤치마킹 사이트의 시각 스타일을 그대로 복제하지 않는다.
- 공통 Core가 업데이트되어도 에코디 정체성 레이어는 안정적으로 유지되어야 하며, 반대로 브랜드 수정이 관리자 작업성을 훼손해서도 안 된다.

## 17. 캐릭터는 장식이 아니라 기능적 인터페이스다
- 에코디언은 항상 떠 있는 장식물로 사용하지 않는다.
- 관리자 화면에서는 기본적으로 조용한 AI 직원처럼 동작하며 도움말, 비어 있는 상태, 오류 복구, 온보딩, 완료 확인에 우선 사용한다.
- 캐릭터가 표, 폼, KPI, 버튼, 알림, 중요 텍스트 위를 덮는 배치는 금지한다.
- 캐릭터가 없어도 모든 관리 기능은 완전하게 사용할 수 있어야 한다.
- 모션은 prefers-reduced-motion 설정을 존중하고, 운영 작업 중 반복 애니메이션을 사용하지 않는다.

## 18. 반복 수정을 시스템 결함으로 전환한다
- 동일 유형의 수정 요청이 두 개 이상의 화면에서 반복되면 개별 페이지 패치가 아니라 공통모듈·토큰·검증 규칙의 결함으로 분류한다.
- 상단 공백, 사이드바 스크롤, 글자 크기, 버튼 크기, 카드 여백, 모바일 잘림, 캐릭터 겹침 등은 공통 가드레일 검사 대상으로 승격한다.
- 개별 서비스의 예외는 명시적인 override 사유가 있어야 하며 기본값보다 먼저 적용되어서는 안 된다.

## 19. 내비게이션은 최대 세 단계까지만 허용한다
- 1단계는 좌측 다섯 축, 2단계는 우측 상단 맥락 탭, 3단계는 서비스 작업공간 내부의 탭·필터·도구막대다.
- 좌측 메뉴 안에 2차·3차 트리를 다시 만들지 않는다.
- 3단계 아래의 세부정보는 새 내비게이션이 아니라 드로어, 모달, 인라인 확장으로 처리한다.

## 20. 모든 서비스는 같은 작업공간 문법을 사용한다
- 기본 순서는 `맥락 탭 → 작업공간 헤더 → 선택적 KPI/상태 → 주 작업영역 → 필요 시 상세 패널`이다.
- 작업공간 헤더에는 제목, 한 줄 목적, 현재 상태, 가장 중요한 1~3개 조작을 우선 배치한다.
- 페이지마다 대형 Hero, 중복 브랜드 헤더, 별도 콘솔 제목막대를 새로 만들지 않는다.

## 21. 밀도는 네 프로필로 조정하고 가독성 하한은 고정한다
- `standard`는 기본, `compact`는 고밀도 업무, `low-height`는 낮은 화면, `mobile`은 재배치 프로필이다.
- 공간이 부족하면 장식·보조문구·간격을 먼저 줄이고 핵심 기능이나 읽을 수 있는 글자 크기를 희생하지 않는다.
- 낮은 화면 때문에 좌측 메뉴 스크롤을 다시 허용하는 방식은 사용하지 않는다.

## 22. 서비스 개성은 Shell 교체가 아니라 Theme Token으로 표현한다
- 서비스별 허용 차이는 포인트색, 서비스 배지, 데이터 시각화 의미색 등 좁은 범위로 제한한다.
- 전역 내비게이션, Shell 구조, 밝고 읽기 쉬운 운영 표면, 공통 컴포넌트의 기본 문법은 서비스가 바꾸지 않는다.
- 관리자 서비스마다 별도 dark/light 콘솔을 만드는 패턴은 금지한다.

## 23. AI는 메뉴를 대체하지 않는 맥락형 동료다
- AI는 검색, 설명, 요약, 초안, 진단, 다음 행동 추천을 빠르게 해 주되 사람이 따라갈 수 있는 메뉴 경로를 항상 남긴다.
- 삭제, 결제, 권한, 배포, 보안 같은 고위험 작업은 기존 승인·감사·권한 경계를 우회하지 않는다.
- AI 패널은 기본적으로 필요할 때 열고, 표·폼·버튼·상태정보를 가리는 상시 부유 UI로 만들지 않는다.

## 24. 공통 UI의 소유권을 코드 수준에서 분리한다
- `admin-menu-registry.js`는 정보구조, `admin-sidebar.js`는 1·2차 내비게이션, `admin-design-engine.*`는 Shell·토큰·가드레일, 각 `*-admin.*`은 서비스 작업내용을 소유한다.
- 서비스 CSS가 Shell의 위치·스크롤·전역 메뉴를 수정하지 못하게 한다.
- 공통 규칙과 실제 런타임이 충돌하면 문서가 아니라 런타임을 즉시 수정하고 CI가 재발을 막아야 한다.

## 25. 관리자 메뉴 변경은 운영 레지스트리 수렴까지가 배포다
- `admin-menu-registry.js`에 공개 관리자 메뉴가 추가·변경·삭제되면 Shared Site Core의 운영 배포가 반드시 뒤따라야 한다.
- 소스의 `adminMenuOrder()`와 운영 `https://ekodi.kr/admin-menu-registry.js`가 일치하지 않으면 배포는 완료가 아니라 `deployed-awaiting-production-verification` 또는 실패 상태로 기록한다.
- 운영 레지스트리가 소스에 수렴한 뒤 실제 인증된 관리자 E2E에서 등록 메뉴 전체를 클릭·렌더링해 통과해야 `production-verified-complete`로 승격한다.
- 오래된 운영 자산, 누락 메뉴, legacy redirect의 정상 동작을 장애로 오판하는 감시 규칙은 발견 즉시 현재 canonical 계약으로 고친다.
- 운영 검증 실패를 피하기 위해 메뉴를 검사 대상에서 제외하거나 테스트 기대치를 낮추는 방식은 금지한다. 실제 배포·라우팅·자산 수렴 문제를 해결한다.
