# EKODI 관리자 UI 원칙 v2

관리자 화면은 ‘관제센터처럼 보이는 것’보다 ‘빠르게 읽고 판단하고 실행하는 것’을 우선한다. 모든 관리자 하위서비스는 이 원칙을 기본값으로 상속하며, 개별 서비스가 별도의 전역 내비게이션 체계를 만들지 않는다.

## 1. 전역 내비게이션은 다섯 축으로 고정한다
관리자 전역의 1차 메뉴는 다음 다섯 개만 사용한다.

1. `홈` — 관리자 진입점과 현재 핵심 상태
2. `운영` — 지금 진행 중인 일, 업무, 결제·회계, 세금·증빙, 소통
3. `공간` — 조직, 고객, 클라우드 자료와 운영공간
4. `서비스` — 마케팅·AI·출판·커뮤니티·소셜 등 개별 서비스
5. `시스템` — 건강, 보안, 기기, 구조, 관리자 권한, 비용, 저장소

새 기능을 만들 때 여섯 번째 전역 메뉴를 추가하지 않는다. 먼저 다섯 축 가운데 의미가 가장 가까운 곳에 배치한다.

## 2. Context를 먼저 보여준다
- 사용자가 다섯 축 중 하나를 선택하면 그 축에 필요한 하위 도구만 보여준다.
- 모든 하위서비스를 한 번에 나열하지 않는다.
- 현재 위치와 관련 없는 메뉴는 접어 시각적·인지적 부하를 줄인다.
- 최근 사용 기능과 즐겨찾기는 빠른 재진입을 돕는 보조 경로로만 제공한다.

## 3. 서비스의 세부 기능은 서비스 내부에서 해결한다
- 전역 사이드바는 서비스 이름까지만 책임진다.
- 캠페인, 승인, CRM, 정산, 설정처럼 서비스 고유 기능은 해당 서비스의 탭·도구막대·점진적 공개 영역에 둔다.
- 한 서비스의 세부 기능 때문에 전역 메뉴가 늘어나지 않게 한다.

## 4. 권한 없는 기능은 숨기고 경로는 결정적으로 유지한다
- 역할·조직·공간 권한이 없는 메뉴와 조작은 노출하지 않는다.
- AI 명령창은 빠른 지름길이지만 유일한 접근수단이 아니다.
- 동일 작업에는 언제나 사람이 따라갈 수 있는 결정적인 메뉴 경로가 존재해야 한다.
- 삭제·결제·권한·핵심 설정 등 위험 작업은 기존 승인·감사 경계를 유지한다.

## 5. 하나의 Navigation Registry만 사용한다
- 관리자 메인과 모든 하위서비스는 `admin-menu-registry.js`와 공통 Sidebar를 사용한다.
- 개별 서비스가 메뉴 이름, 순서, 그룹, 번역을 독자적으로 복제하지 않는다.
- 새로운 관리자 기능은 Registry에 등록해야 하며 다섯 축 중 하나에 반드시 소속되어야 한다.
- 내부 운영 기능은 필요하면 AI·진단 경로로 연결하되 별도 전역 축으로 승격하지 않는다.

## 6. 정보가 장식보다 먼저다
- 관리자 메인 배경은 밝은 중립색, 데이터·작업 표면은 흰색을 기본으로 한다.
- 큰 면적의 짙은 배경, 그라데이션, 글로우, 과도한 그림자는 사용하지 않는다.
- 짙은 배경은 코드·보안 경고·특수 진단처럼 의미가 분명한 좁은 영역에만 제한한다.

## 7. 한눈에 읽히는 문자 체계
- 본문·표·버튼은 데스크톱에서 13~15px 이상을 기본으로 하고, 핵심 본문은 14~16px를 권장한다.
- KPI 값은 20~28px, 섹션 제목은 17~24px 범위에서 계층을 명확히 한다.
- 보조문구도 11px 미만으로 축소하지 않는다. 운영정보를 ‘작게 숨기는’ 방식은 금지한다.
- 본문 대비는 WCAG AA 수준을 목표로 한다.

## 8. 8px 계열 간격과 낮은 시각 밀도
- 기본 간격은 8px 계열로 구성하고 카드 사이 12~16px, 섹션 안쪽 14~24px를 기준으로 한다.
- 같은 의미의 숫자·상태·조작은 동일한 위치와 리듬으로 반복한다.
- 비어 있는 공간은 정보 구획을 위해 사용하되, 빈 화면을 만들기 위한 큰 최소높이는 두지 않는다.

## 9. 상태는 색이 아니라 문장으로도 이해되어야 한다
- 정상·주의·오류 색은 의미가 있는 상태에만 사용한다.
- 색상만으로 상태를 구별하지 않고 텍스트 라벨을 함께 표시한다.
- 원시 오류코드만 노출하지 않는다. 사용자 문장을 먼저 보여주고 필요하면 기술코드를 보조정보로 둔다.

## 10. 로딩·빈 상태·인증·오류를 화면의 일부로 설계한다
- 빈 데이터 영역을 거대한 공백으로 남기지 않는다.
- 로딩, 데이터 없음, 인증 필요, 연결 대기, 오류를 각각 명시적인 상태 카드로 보여준다.
- 실패 상태에서도 다음 행동이 무엇인지 알 수 있어야 한다.

## 11. 반응형은 ‘축소’가 아니라 ‘재배치’다
- 900px 이하에서는 툴바와 2열 콘텐츠를 1열로 전환한다.
- 620px 이하에서는 KPI를 2열 중심으로 배치하고 조작 버튼은 충분한 터치 영역을 확보한다.
- 표와 탭을 제외하고 중첩 스크롤 영역을 만들지 않는다.
- 다섯 전역 축은 모바일에서도 의미와 순서를 유지한다.

## 12. 경량성이 기능이다
- 첫 관리자 진입에는 공통 쉘과 현재 화면에 필요한 최소 자산만 로드한다.
- 하위서비스 CSS·JS는 메뉴 진입 시 지연 로딩한다.
- UI 개선을 위해 무거운 프레임워크, 아이콘 패키지, 애니메이션 라이브러리를 추가하지 않는다.
- CSS 그림자·블러·배경효과는 최소화하고 브라우저 기본 레이아웃 기능을 우선한다.

## 13. 공통 토큰을 깨지 않는다
- 페이지 배경, 표면, 본문색, 보조색, 테두리, 포인트색, 성공·주의·오류색, 반경, 간격, 컨트롤 높이는 공통 관리자 토큰을 따른다.
- 하위서비스가 고유 브랜드색을 쓸 수는 있지만 공통 가독성·간격·상태 규칙을 덮어쓰지 않는다.

## 14. 배포 전 검증 기준
- 전역 1차 메뉴가 `홈 · 운영 · 공간 · 서비스 · 시스템` 다섯 축인지 자동 검증한다.
- 모든 공개 관리자 메뉴가 다섯 축 중 하나에 소속되는지 검증한다.
- 데스크톱 1366×768 및 모바일 390×844에서 핵심 정보가 잘리지 않아야 한다.
- 본문·KPI·탭·상태 라벨이 육안으로 구별되어야 한다.
- 인증·빈 상태·오류에서 거대한 빈 패널이 생기지 않아야 한다.
- 첫 관리자 경로의 기존 JS/CSS 성능 예산과 true-lazy 계약을 유지해야 한다.
- production 배포 후 `admin.ekodi.kr` 응답, fingerprint, immutable lazy assets와 다섯 축 내비게이션 자산을 확인한다.
