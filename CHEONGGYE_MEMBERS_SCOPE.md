# 청계면상인회 정회원 명단 사용 범위

이 문서는 `상인회정회원현황(회비납부순)` 구글시트의 `웹관리` 탭과 청계면상인회 정회원 관리 기능에 적용되는 소유권 원칙이다.

## 소유 주체

- 명단 소유 주체: 청계면상인회
- 명단 성격: 청계면상인회 소속 정회원 및 공모전 대상 상가 명단
- 기준 원본: Google Sheets `상인회정회원현황(회비납부순)` 파일의 `웹관리` 탭

## 사용 제한

이 명단은 청계면상인회 관련 업무, 공모전, 주민제안사업, 상권활성화, 상인회 내부 운영 목적으로만 사용한다.

다른 기관, 단체, 학교, 협의체, 조합, 프로젝트, 고객사, 테넌트, 워크스페이스의 공용 명단 또는 기본 샘플 데이터로 재사용하지 않는다.

## 관리자 진입 원칙

- canonical 소유 관리자 경로는 `https://ekodi.kr/cgma/admin/member`이다.
- 기존 `https://admin.ekodi.kr/#cheonggye-members`는 전환 기간 호환 진입으로만 유지하며 중앙 기본 메뉴에는 노출하지 않는다.
- 일반 회원 로그인·정회원 신청 주소 `https://ekodi.kr/cgma/member`는 별도 사용자 화면으로 유지한다.

## 시스템 적용 원칙

- 화면명과 설명에는 `청계면상인회 소속 정회원 명단`임을 명확히 표시한다.
- 저장소, API, 시트 연동, 캐시 키, 권한 설정은 `cheonggye-merchant-association` 범위로 분리한다.
- 다른 기관·단체가 같은 기능을 사용할 경우, 별도 workspace_id, 별도 시트, 별도 권한, 별도 데이터 소스를 연결한다.
- 청계면상인회 명단을 복제하여 다른 단체의 초기 데이터로 사용하지 않는다.

## 권한

사전등록된 관리자만 접근하며, 접근 권한은 청계면상인회 운영공간 권한으로 분리 관리한다.
