const BIZINFO_DEFAULT_URL='https://www.bizinfo.go.kr/uss/rss/bizinfoApi.do';
const clean=v=>String(v??'').replace(/<[^>]*>/g,' ').replace(/\s+/g,' ').trim();
const splitTags=v=>clean(v).split(',').map(s=>s.trim()).filter(Boolean);
export function normalizeBizinfoItem(item={}){const tags=splitTags(item.hashTags||item.hashTagsNm||'');const period=clean(item.reqstBeginEndDe||item.reqstDt||'');return{id:clean(item.pblancId||item.seq),source:'bizinfo',sourceName:'기업마당',official:true,title:clean(item.pblancNm||item.title),url:clean(item.pblancUrl||item.link),agency:clean(item.jrsdInsttNm||item.author),operator:clean(item.excInsttNm),summary:clean(item.bsnsSumryCn||item.description),category:clean(item.pldirSportRealmLclasCodeNm||item.lcategory),target:clean(item.trgetNm),applicationMethod:clean(item.reqstMthPapersCn),applicationUrl:clean(item.rceptEngnHmpgUrl),contact:clean(item.refrncNm),applicationPeriod:period,publishedAt:clean(item.creatPnttm||item.pubDate),attachmentUrl:clean(item.flpthNm||item.printFlpthNm),attachmentName:clean(item.fileNm||item.printFileNm),tags,region:tags.find(t=>['서울','부산','대구','인천','전남광주','대전','울산','세종','경기','강원','충북','충남','전북','경북','경남','제주'].includes(t))||'',industry:'',businessType:clean(item.trgetNm),raw:item}}
export function normalizeBizinfoResponse(payload={}){const root=payload?.jsonArray||payload?.response||payload||{};const items=Array.isArray(root.item)?root.item:root.item?[root.item]:[];return items.map(normalizeBizinfoItem).filter(x=>x.id&&x.title)}
export function officialSourceStatus(env={}){return[{id:'bizinfo',name:'기업마당',official:true,mode:env.BIZINFO_API_KEY?'ready':'credential_required',endpoint:env.BIZINFO_API_URL||BIZINFO_DEFAULT_URL,credential:'BIZINFO_API_KEY',capabilities:['latest-notices','category-filter','hashtag-filter','attachments']},{id:'kstartup',name:'K-Startup',official:true,mode:env.KSTARTUP_API_URL?'ready':'configured-next',endpoint:env.KSTARTUP_API_URL||'https://www.k-startup.go.kr/',credential:env.KSTARTUP_API_URL?'runtime-configured':'adapter-pending',capabilities:['startup-notices','target-filter','region-filter']} ]}
export async function fetchBizinfoNotices(env={},options={}){if(!env.BIZINFO_API_KEY)return{ok:false,source:'bizinfo',reason:'credential_required',items:[]};const endpoint=new URL(env.BIZINFO_API_URL||BIZINFO_DEFAULT_URL);endpoint.searchParams.set('crtfcKey',env.BIZINFO_API_KEY);endpoint.searchParams.set('dataType','json');endpoint.searchParams.set('searchCnt',String(Math.min(Math.max(Number(options.limit)||100,1),300)));if(options.category)endpoint.searchParams.set('searchLclasId',String(options.category));if(options.hashtags)endpoint.searchParams.set('hashtags',Array.isArray(options.hashtags)?options.hashtags.join(','):String(options.hashtags));const response=await fetch(endpoint.toString(),{headers:{accept:'application/json'},cf:{cacheTtl:300,cacheEverything:true}});if(!response.ok)return{ok:false,source:'bizinfo',reason:`upstream_${response.status}`,items:[]};let payload;try{payload=await response.json()}catch{return{ok:false,source:'bizinfo',reason:'invalid_json',items:[]}}return{ok:true,source:'bizinfo',fetchedAt:new Date().toISOString(),items:normalizeBizinfoResponse(payload)}}
