import { injectEkodiShell } from './ekodi-shell-injector.js';
import { SUPPORT_STAGES, analyzeGuidanceChange, scoreOpportunity, fillOfficialForm, buildNextActions, requiresHumanGate, rankOpportunities, buildProactiveBrief } from './support/core.js';
import { officialSourceStatus, fetchBizinfoNotices } from './support/sources.js';
const SECURITY_HEADERS={'x-content-type-options':'nosniff','referrer-policy':'strict-origin-when-cross-origin','permissions-policy':'camera=(), microphone=(), geolocation=()','content-security-policy':"default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data: https:; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; upgrade-insecure-requests"};
function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store',...SECURITY_HEADERS}})}
function withHeaders(response){const headers=new Headers(response.headers);for(const[key,value]of Object.entries(SECURITY_HEADERS))headers.set(key,value);if(!headers.has('cache-control'))headers.set('cache-control',response.headers.get('content-type')?.includes('text/html')?'no-cache':'public, max-age=300');return new Response(response.body,{status:response.status,statusText:response.statusText,headers})}
async function body(request){try{return await request.json()}catch{return null}}
function runtimeConfig(env){const sources=officialSourceStatus(env);return{dataMode:env.DATA_MODE||'isolated-staging',authUrl:env.AUTH_URL||'https://auth.ekodi.kr/?site=support',officialSourceRequired:true,officialSources:sources.map(({id,name,mode})=>({id,name,mode})),submissionExecution:false,humanGateRequired:true,persistence:'browser-local-first',proactiveBriefing:true}}
async function getOfficialNotices(env,url){const source=url.searchParams.get('source')||'bizinfo';if(source!=='bizinfo')return{ok:false,source,reason:'source_not_enabled',items:[]};return fetchBizinfoNotices(env,{limit:url.searchParams.get('limit')||100,category:url.searchParams.get('category')||'',hashtags:url.searchParams.get('hashtags')||''})}
export default{async fetch(request,env){const url=new URL(request.url);if(url.pathname==='/health')return json({ok:true,service:'ekodi-support-opportunity',surface:'support-platform',stages:SUPPORT_STAGES,officialSourceRequired:true,officialSources:officialSourceStatus(env).map(({id,mode})=>({id,mode})),submissionExecution:false,humanGateRequired:true,dataMode:runtimeConfig(env).dataMode,ekodiShell:true});if(url.pathname==='/config.js')return new Response(`window.EKODI_SUPPORT_CONFIG=${JSON.stringify(runtimeConfig(env))};`,{headers:{'content-type':'application/javascript; charset=utf-8','cache-control':'no-store',...SECURITY_HEADERS}});if(url.pathname==='/api/sources/status'&&request.method==='GET')return json({sources:officialSourceStatus(env)});if(url.pathname==='/api/opportunities'&&request.method==='GET'){const result=await getOfficialNotices(env,url);return json(result,result.ok?200:result.reason==='credential_required'?503:502)}if(url.pathname==='/api/opportunities/match'&&request.method==='POST'){const p=await body(request);if(!p?.profile)return json({error:'profile_required'},400);let notices=p.notices;if(!Array.isArray(notices)){const upstream=await fetchBizinfoNotices(env,{limit:p.limit||100,category:p.category||'',hashtags:p.hashtags||''});if(!upstream.ok)return json(upstream,upstream.reason==='credential_required'?503:502);notices=upstream.items}return json({matches:rankOpportunities(p.profile,notices,{minScore:p.minScore,now:p.now}),source:Array.isArray(p.notices)?'provided':'bizinfo'})}if(url.pathname==='/api/proactive-brief'&&request.method==='POST'){const p=await body(request);if(!p?.profile)return json({error:'profile_required'},400);let notices=Array.isArray(p.notices)?p.notices:null;if(!notices){const upstream=await fetchBizinfoNotices(env,{limit:p.limit||100,category:p.category||'',hashtags:p.hashtags||''});if(!upstream.ok)return json({...upstream,brief:buildProactiveBrief(p.profile,[],p.projects||[],{minScore:p.minScore,now:p.now})},upstream.reason==='credential_required'?503:502);notices=upstream.items}return json(buildProactiveBrief(p.profile,notices,p.projects||[],{minScore:p.minScore,now:p.now}))}if(url.pathname==='/api/change-analysis'&&request.method==='POST'){const p=await body(request);if(!p)return json({error:'invalid_json'},400);return json(analyzeGuidanceChange(p.previous,p.current))}if(url.pathname==='/api/opportunity-score'&&request.method==='POST'){const p=await body(request);if(!p)return json({error:'invalid_json'},400);return json({score:scoreOpportunity(p.profile,p.notice)})}if(url.pathname==='/api/form-fill'&&request.method==='POST'){const p=await body(request);if(!p||!Array.isArray(p.schema))return json({error:'invalid_form_schema'},400);return json({fields:fillOfficialForm(p.schema,p.profile,p.project)})}if(url.pathname==='/api/next-actions'&&request.method==='POST'){const p=await body(request);return json({actions:buildNextActions(p||{})})}if(url.pathname==='/api/action-gate'&&request.method==='POST'){const p=await body(request);const action=p?.action||'';return json({action,humanGateRequired:requiresHumanGate(action),allowedAutonomously:!requiresHumanGate(action)})}if(url.pathname==='/admin'||url.pathname==='/admin/')return Response.redirect('https://admin.ekodi.kr/',307);const response=await env.ASSETS.fetch(request);return injectEkodiShell(withHeaders(response),'support')}};
