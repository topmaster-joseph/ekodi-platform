import {readFile,writeFile} from 'node:fs/promises';
const path='store-admin-engine.js';
let s=await readFile(path,'utf8');
const replaceOne=(from,to,label)=>{if(!s.includes(from))throw new Error(`patch anchor missing: ${label}`);s=s.replace(from,to)};

replaceOne(
"  const state={session:null,snapshot:null,menu:null,connector:null,site:null,role:''};",
"  const state={session:null,snapshot:null,menu:null,connector:null,site:null,role:'',platformOps:null,commerce:null};",
'state'
);

replaceOne(
`  function platformImportPanel(){return \`<section class="site-lifecycle"><h2>배달앱 메뉴 스냅샷 등록</h2><p class="panel-lead">땡겨요·배달의민족·요기요·먹깨비에서 직접 확인한 목포대점 메뉴 JSON만 등록합니다. 이미지·가격·주문 링크는 검증된 값만 고객페이지에 공개됩니다.</p><form id="platformMenuForm"><div class="site-grid"><label class="site-field"><strong>배달앱</strong><select name="provider"><option value="ddangyo">땡겨요</option><option value="baemin">배달의민족</option><option value="yogiyo">요기요</option><option value="mukkebi">먹깨비</option><option value="coupang_eats">쿠팡이츠</option></select></label><label class="site-field"><strong>목포대점 주문 URL</strong><input name="store_order_url" type="url" placeholder="https://..."></label><label class="site-field"><strong>확인한 원본 URL</strong><input name="source_url" type="url" placeholder="https://..."></label><label class="site-field"><strong>앱의 매장명</strong><input name="platform_store_name" placeholder="피자마루 목포대점"></label><label class="site-field site-wide"><strong>메뉴 JSON</strong><textarea name="items_json" rows="9" placeholder='[{"name":"콤비네이션 피자","price":15900,"image_url":"https://...","order_url":"https://..."}]'></textarea></label></div><div class="actions"><button class="button primary" type="submit">검증 스냅샷 등록</button></div></form></section>\`}`,
`  function platformImportPanel(){return \`<section class="site-lifecycle"><h2>플랫폼 메뉴 스냅샷 등록</h2><p class="panel-lead">땡겨요·배달의민족·요기요·먹깨비·쿠팡이츠·당근주문·네이버주문에서 직접 확인하거나 승인된 원본만 등록합니다. 이미지·가격·주문 링크는 검증된 값만 고객페이지에 공개됩니다.</p><form id="platformMenuForm"><div class="site-grid"><label class="site-field"><strong>플랫폼</strong><select name="provider"><option value="ddangyo">땡겨요</option><option value="baemin">배달의민족</option><option value="yogiyo">요기요</option><option value="mukkebi">먹깨비</option><option value="coupang_eats">쿠팡이츠</option><option value="daangn">당근주문</option><option value="naver_order">네이버주문</option></select></label><label class="site-field"><strong>점포 주문 URL</strong><input name="store_order_url" type="url" placeholder="https://..."></label><label class="site-field"><strong>확인한 원본 URL</strong><input name="source_url" type="url" placeholder="https://..."></label><label class="site-field"><strong>플랫폼 매장명</strong><input name="platform_store_name" placeholder="${STORE_NAME}"></label><label class="site-field site-wide"><strong>메뉴 JSON</strong><textarea name="items_json" rows="9" placeholder='[{"name":"콤비네이션 피자","price":15900,"image_url":"https://...","order_url":"https://..."}]'></textarea></label></div><div class="actions"><button class="button primary" type="submit">검증 스냅샷 등록</button></div></form></section>\`}`,
'platform import'
);

replaceOne(
`  function ordersPanel(){
    const channels=Object.entries(state.snapshot?.orders?.channels||{});const rows=channels.map(([name,v])=>\`<tr><td>\${esc(name)}</td><td>\${num(v.orders)}</td><td>\${won(v.sales)}</td><td>\${v.orders?won(Number(v.sales||0)/Number(v.orders)):won(0)}</td></tr>\`).join('');
    $('mainPanel').innerHTML=\`<h2>주문 · 채널 집계</h2><p class="panel-lead">주문 상세가 아니라 오늘 완료 주문의 채널별 집계만 제공합니다.</p>\${rows?\`<div class="table-wrap"><table><thead><tr><th>채널</th><th>주문</th><th>매출</th><th>객단가</th></tr></thead><tbody>\${rows}</tbody></table></div>\`:'<div class="empty"><strong>오늘 주문 집계가 없습니다.</strong>주문이 없거나 원장 연결이 아직 준비되지 않았습니다.</div>'}\`;
  }`,
`  function ordersPanel(){
    const p=state.platformOps||{},sum=p.summary||{},orders=Array.isArray(p.orders)?p.orders:[];const labels={ddangyo:'땡겨요',baemin:'배달의민족',yogiyo:'요기요',mukkebi:'먹깨비',coupang_eats:'쿠팡이츠',daangn:'당근주문',naver_order:'네이버주문',store:'본사 주문'};
    const detail=orders.map(o=>\`<tr><td>\${esc(labels[o.provider]||o.provider)}</td><td>\${esc(o.order_no||'—')}</td><td>\${esc(new Date(o.ordered_at).toLocaleString('ko-KR'))}</td><td>\${esc(o.status||'unknown')}</td><td>\${won(o.total)}</td><td>\${esc(o.delivery_area_hint||'—')}</td><td>\${o.order_url?\`<a href="\${esc(o.order_url)}" target="_blank" rel="noopener">플랫폼 열기</a>\`:'—'}</td></tr>\`).join('');
    const channels=Object.entries(state.snapshot?.orders?.channels||{}),rows=channels.map(([name,v])=>\`<tr><td>\${esc(labels[name]||name)}</td><td>\${num(v.orders)}</td><td>\${won(v.sales)}</td><td>\${v.orders?won(Number(v.sales||0)/Number(v.orders)):won(0)}</td></tr>\`).join('');
    $('mainPanel').innerHTML=\`<h2>주문 · 배달내역</h2><p class="panel-lead">각 플랫폼의 승인된 주문 원장을 한 화면에서 조회합니다. 고객 전화번호·상세주소 원문은 EKODI에 장기 저장하지 않습니다.</p><div class="mini-grid"><div class="mini"><small>최근 \${num(p.window_days||30)}일 주문</small><strong>\${num(sum.orders)}</strong></div><div class="mini"><small>완료 매출</small><strong>\${won(sum.gross_sales)}</strong></div><div class="mini"><small>순수취 예상</small><strong>\${won(sum.net_receivable)}</strong></div><div class="mini"><small>오늘 주문</small><strong>\${num(state.snapshot?.orders?.count)}</strong></div></div>\${detail?\`<h2 class="site-lifecycle">플랫폼 주문 상세</h2><div class="table-wrap"><table><thead><tr><th>플랫폼</th><th>주문번호</th><th>주문시각</th><th>상태</th><th>결제액</th><th>배달지역</th><th>원본</th></tr></thead><tbody>\${detail}</tbody></table></div>\`:''}<h2 class="site-lifecycle">오늘 채널 집계</h2>\${rows?\`<div class="table-wrap"><table><thead><tr><th>채널</th><th>주문</th><th>매출</th><th>객단가</th></tr></thead><tbody>\${rows}</tbody></table></div>\`:'<div class="empty"><strong>연결된 주문이 없습니다.</strong>연결관리에서 공식 API·점주 계정·승인 브라우저 또는 검증 파일을 연결해 주세요.</div>'}\`;
  }`,
'orders panel'
);

replaceOne(
`  function reviewsPanel(){const mk=state.snapshot?.marketing||{};$('mainPanel').innerHTML=\`<h2>리뷰 운영</h2><div class="mini-grid"><div class="mini"><small>미응답 리뷰</small><strong>\${num(mk.unansweredReviews)}</strong></div><div class="mini"><small>Marketing 집계</small><strong>\${mk.connected?'연결됨':'대기'}</strong></div></div><div class="actions"><a class="button primary" href="\${STORE_BASE}/marketing">Marketing AI 열기</a></div><div class="note">답변 초안은 AI가 도울 수 있지만 고객에게 공개 발송하기 전 사람의 검토를 유지합니다.</div>\`}`,
`  function reviewsPanel(){const mk=state.snapshot?.marketing||{},p=state.platformOps||{},sum=p.summary||{},reviews=Array.isArray(p.reviews)?p.reviews:[];const labels={ddangyo:'땡겨요',baemin:'배달의민족',yogiyo:'요기요',mukkebi:'먹깨비',coupang_eats:'쿠팡이츠',daangn:'당근주문',naver_order:'네이버주문',store:'본사'};const rows=reviews.map(r=>\`<div class="service-row" style="align-items:flex-start"><div style="min-width:0;flex:1"><strong>\${esc(labels[r.provider]||r.provider)} · ★ \${esc(r.rating??'—')} · \${esc(r.author_alias||'고객')}</strong><p>\${esc(r.body||'리뷰 내용 없음')}</p><label class="site-field site-wide" style="margin-top:8px"><strong>답글 초안</strong><textarea rows="3" data-review-draft="\${esc(r.id)}">\${esc(r.reply_draft||r.reply_body||'')}</textarea></label><div class="actions"><button class="button" type="button" data-save-review="\${esc(r.id)}">초안 저장</button><button class="button primary" type="button" data-queue-review="\${esc(r.id)}">검토 후 플랫폼 발송 대기</button>\${r.url?\`<a class="button" href="\${esc(r.url)}" target="_blank" rel="noopener">원본 리뷰</a>\`:''}</div></div><span class="tag \${r.reply_status==='published'?'live':r.reply_status==='failed'?'bad':'warn'}">\${esc(r.reply_status||'unanswered')}</span></div>\`).join('');$('mainPanel').innerHTML=\`<h2>플랫폼 리뷰 · 답글</h2><p class="panel-lead">각 배달플랫폼 리뷰를 한곳에서 조회하고 답글을 준비합니다. AI·운영자 초안은 자동 발송하지 않고 관리자 검토 후에만 플랫폼 발송 대기열에 들어갑니다.</p><div class="mini-grid"><div class="mini"><small>미응답 리뷰</small><strong>\${num(sum.unanswered_reviews??mk.unansweredReviews)}</strong></div><div class="mini"><small>평균 평점</small><strong>\${sum.avg_rating==null?'—':esc(sum.avg_rating)}</strong></div></div><div class="actions"><a class="button" href="\${STORE_BASE}/marketing">Marketing AI 열기</a></div><div class="service-list site-lifecycle">\${rows||'<div class="empty"><strong>연결된 플랫폼 리뷰가 없습니다.</strong>연결관리에서 플랫폼 수집을 연결하면 리뷰와 답글 상태가 나타납니다.</div>'}</div>\`;
    document.querySelectorAll('[data-save-review]').forEach(b=>b.onclick=async()=>{const id=b.dataset.saveReview,t=document.querySelector(`[data-review-draft="\${id}"]`);b.disabled=true;try{await rpc('store_platform_review_save_draft',{p_slug:workspaceSlug,p_review_id:id,p_draft:t?.value||''});await loadData();setState('답글 초안 저장','live')}catch(e){setState('초안 저장 실패','warn');$('pageCopy').textContent=e.message}finally{b.disabled=false}});
    document.querySelectorAll('[data-queue-review]').forEach(b=>b.onclick=async()=>{const id=b.dataset.queueReview,t=document.querySelector(`[data-review-draft="\${id}"]`);if(!String(t?.value||'').trim())return setState('답글을 입력해 주세요','warn');b.disabled=true;try{await rpc('store_platform_review_queue_reply',{p_slug:workspaceSlug,p_review_id:id,p_reply:t.value});await loadData();setState('플랫폼 발송 대기 등록','live')}catch(e){setState('발송 대기 실패','warn');$('pageCopy').textContent=e.message}finally{b.disabled=false}});
  }`,
'reviews panel'
);

replaceOne(
`  function salesPanel(){const s=state.snapshot||{},m=s.metrics||{},o=s.orders||{};$('mainPanel').innerHTML=\`<h2>오늘 매출</h2><div class="mini-grid"><div class="mini"><small>매출</small><strong>\${won(m.sales)}</strong></div><div class="mini"><small>주문</small><strong>\${num(o.count)}</strong></div><div class="mini"><small>객단가</small><strong>\${won(o.averageTicket)}</strong></div><div class="mini"><small>지난주 같은 요일 대비</small><strong>\${m.salesDelta==null?'—':esc((m.salesDelta>=0?'+':'')+m.salesDelta+'%')}</strong></div></div>\`}`,
`  function salesPanel(){const s=state.snapshot||{},m=s.metrics||{},o=s.orders||{},p=state.platformOps||{},sum=p.summary||{},sett=Array.isArray(p.settlements)?p.settlements:[];const rows=sett.map(x=>\`<tr><td>\${esc(x.provider)}</td><td>\${esc(x.period_start)} ~ \${esc(x.period_end)}</td><td>\${won(x.gross_sales)}</td><td>\${won(x.platform_fees)}</td><td>\${won(x.delivery_fees)}</td><td>\${won(x.net_settlement)}</td></tr>\`).join('');$('mainPanel').innerHTML=\`<h2>매출 · 정산</h2><div class="mini-grid"><div class="mini"><small>오늘 통합 매출</small><strong>\${won(m.sales)}</strong></div><div class="mini"><small>오늘 주문</small><strong>\${num(o.count)}</strong></div><div class="mini"><small>최근 플랫폼 매출</small><strong>\${won(sum.gross_sales)}</strong></div><div class="mini"><small>최근 순수취 예상</small><strong>\${won(sum.net_receivable)}</strong></div></div>\${rows?\`<h2 class="site-lifecycle">플랫폼 정산</h2><div class="table-wrap"><table><thead><tr><th>플랫폼</th><th>기간</th><th>총매출</th><th>플랫폼비</th><th>배달비</th><th>정산액</th></tr></thead><tbody>\${rows}</tbody></table></div>\`:'<div class="note">정산 원장이 연결되면 플랫폼별 총매출·수수료·배달비·실정산액을 비교합니다.</div>'}\`}`,
'sales panel'
);

replaceOne(
`    const [snapshot,connector]=await Promise.all([rpc('business_os_store_admin_snapshot',{p_workspace_key:SLUG}),control('/api/marketing/connectors/status?store='+encodeURIComponent(STORE_ID))]);
    state.snapshot=snapshot;state.connector=connector;render();`,
`    const [snapshot,connector,platformOps,commerce]=await Promise.all([rpc('business_os_store_admin_snapshot',{p_workspace_key:SLUG}),control('/api/marketing/connectors/status?store='+encodeURIComponent(STORE_ID)),rpc('store_delivery_platform_admin_snapshot',{p_slug:workspaceSlug,p_days:30}).catch(()=>null),rpc('delivery_commerce_admin_snapshot',{p_slug:workspaceSlug}).catch(()=>null)]);
    state.snapshot=snapshot;state.connector=connector;state.platformOps=platformOps;state.commerce=commerce;render();`,
'load data'
);

await writeFile(path,s,'utf8');
console.log('Patched store-admin-engine.js with Delivery Commerce Intelligence UI.');
