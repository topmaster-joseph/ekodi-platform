name: Retired - Activate EKODI Pages Domains
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired topology action. EKODI-owned Pages subdomains are no longer activated."

