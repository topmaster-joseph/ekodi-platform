# Development Baseline Refresh

When `development` is behind `main`, refresh the production baseline into `development` before promoting new work. The refresh must preserve development-only Cloudflare account, Worker, and secret boundaries.
