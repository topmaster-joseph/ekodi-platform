# Development Next Steps

1. Keep `development` synchronized with the latest `main` baseline.
2. Run development deployment and post-deploy verification after code changes.
3. Require boundary audit before promotion.
4. Promote only through a reviewed pull request into `main`.
