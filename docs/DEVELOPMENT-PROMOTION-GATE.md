# Development Promotion Gate

Promotion from `development` to `main` is allowed only after the current development commit has passed deployment, endpoint verification, and boundary audit. A baseline sync from `main` into `development` should be completed first when development is behind production.
