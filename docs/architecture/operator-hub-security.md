# Operator Hub Security Boundary

My EKODI is a projection over existing authorization, not an authority. All site/service admin links and operational summaries must be filtered by server-validated permissions. The hub must not expose hidden admin destinations to unauthorized users, and URL discovery must never confer access.
