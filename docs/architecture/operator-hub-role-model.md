# Operator Hub Role Model

The operator hub may be used by platform super administrators, deputy operators, service owners, and delegated site administrators. Visibility is person-scoped and permission-derived. Role labels are descriptive projections of existing authorization; they are not a new authorization database.
