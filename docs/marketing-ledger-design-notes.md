# Design constraints

The ledger deliberately avoids raw contact identity, arbitrary customer metadata and external execution. It stores only workspace-scoped pseudonyms and operational event facts. Campaign publication remains outside this API and requires the existing Human Gate workflow.
