# Main Baseline Synchronization

Before promoting development work, refresh the development baseline from `main` when the branches have diverged. This keeps validation representative of current production while preserving development-only Cloudflare boundaries.
