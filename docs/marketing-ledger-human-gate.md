# Campaign Human Gate

Campaign review creates an AI Mission Control action with `human_gate` and `awaiting_human`. The Marketing ledger API intentionally has no send, publish, execute or approval endpoint. External action remains a separately governed step.
