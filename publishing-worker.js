import { injectEkodiShell } from './ekodi-shell-injector.js';
function assetRequest(request,pathname){const url=new URL(request.url);url.pathname=pathname;return new Request(url,request)}
export default {async fetch(request,env){const url=new URL(request.url);if(url.pathname==='/health'){return new Response(JSON.stringify({ok:true,service:'ekodi-publishing',professionalService:true,modules:['consultation','production','distribution','studio','upaper']}),{headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}})}if(url.pathname==='/admin'||url.pathname==='/admin/'||url.pathname==='/admin.html'){return Response.redirect('https://admin.ekodi.kr/publishing#publishing',307)}if(url.pathname==='/books.json'){const upstream=await fetch('https://books.ekodi.kr/books.json',{headers:{accept:'application/json'}});return new Response(upstream.body,{status:upstream.status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}})}if(url.pathname==='/publishing/'||url.pathname==='/publishing'){return Response.redirect('https://publishing.ekodi.kr/',308)}if(url.pathname.startsWith('/publishing/studio'))return Response.redirect('https://publishing.ekodi.kr/studio/',308);if(url.pathname.startsWith('/publishing/upaper'))return Response.redirect('https://publishing.ekodi.kr/upaper/',308);return injectEkodiShell(await env.ASSETS.fetch(request),'publishing')}};