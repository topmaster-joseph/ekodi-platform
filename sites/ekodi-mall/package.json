{
  "name": "ekodi-mall-pages",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check assets/seller.js && node --check assets/seller-server.js && node --check assets/public-product.js && node --check assets/marketplace-live.js && node --check assets/app.js && node --check assets/commerce.js && node --check api/worker.js && node --test api/worker.test.mjs && node scripts/validate.mjs",
    "build": "npm run check && node scripts/build.mjs && node scripts/verify-build.mjs",
    "verify": "node scripts/verify-build.mjs",
    "doctor": "npm run build"
  },
  "engines": { "node": ">=22" }
}