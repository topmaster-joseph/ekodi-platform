{
  "name": "ekodi-mall-pages",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/validate.mjs",
    "build": "npm run check && node scripts/build.mjs && node scripts/verify-build.mjs",
    "verify": "node scripts/verify-build.mjs",
    "doctor": "npm run build"
  },
  "engines": {
    "node": ">=22"
  }
}
