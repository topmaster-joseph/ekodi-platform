name = "ekodi-ai-control-build"
main = "ai-control-entry-worker.js"
compatibility_date = "2026-09-01"
workers_dev = false
preview_urls = false

# Build-only source of the application bundle.
# Runtime bindings, routes, secrets and assets are intentionally excluded.
# The resulting bundle is promoted unchanged through staging and production.
