name = "ekodi-service-proxy"
main = "service-proxy.js"
compatibility_date = "2026-08-11"
workers_dev = false
preview_urls = false

[vars]
ENVIRONMENT = "production"

# No public custom domains. This Worker may be reached only through private
# service bindings or explicitly governed internal execution paths.
